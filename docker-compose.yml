version: '3.9'
services:
  plotterlab:
    build: .
    image: plotterlab:latest
    container_name: plotterlab
    ports:
      - "8082:8080"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
